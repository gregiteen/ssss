# Dependencies
node_modules/

# Local agent state; skill packages remain versionable
.claude/
.agent/*
!.agent/skills/
.agent/skills/**/.git/
.agent/skills/**/.check.lock
.agent/skills/**/reports/
.agent/skills/total-recall/
.agents/*
!.agents/skills/
.remember/
.gemini/
INSTRUCTIONS.md
CLAUDE.md

# OS / editor cruft
.DS_Store
*.swp
*~

# Generated bundles / scratch (the canonical reference bundle is committed explicitly)
*.ucw.json
!conformance/reference-bundle.ucw.json
.agent/scratch/

# Local npm pack artifacts (release candidates are built, not committed)
*.tgz

# Total Recall secrets projections
.env
.env.local
.env.*.local
