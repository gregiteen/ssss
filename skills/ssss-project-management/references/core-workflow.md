# SSSS readiness walkthrough

Source: this repository's `registry/core.json`, `docs/ssss-spec.md`, `src/kernel.mjs`, and `conformance/fixtures.json`.

For a changed contract, validate a conforming Markdown document, submit a valid operation with a verified principal, replay its idempotency key, reject a conflicting replay, and inspect the canonical event. Check any affected bundle, workflow, lease, or adapter path. Run the repo's full code-quality tier on the final tree and record the result in the active project's verification log.
